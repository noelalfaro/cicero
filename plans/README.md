# Implementation Plans

Generated by the improve skill on 2026-06-10 at commit `c70fb52`, from the Linear backlog
(Courtside team). One plan per ticket, named `plans/CRT-XX.md` per the operator's request.
Execute in the order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

All plans assume: local `npm run dev` hits the **production** Neon DB (read-only browsing
only); `npm run db:push` / any DB write / new dependencies / `git push` require operator
approval; never touch `.env.local`, committed migrations, or `package-lock.json`.

## Execution order & status

| Order | Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|---|
| 1 | [CRT-84](CRT-84.md) | Align Zod/Drizzle schemas with what Combine actually writes | P1 | M | — | TODO |
| 2 | [CRT-83](CRT-83.md) | Verify player pages render with Combine-written stats | P1 | S | CRT-84 | TODO |
| 3 | [CRT-85](CRT-85.md) | PR Score time-series chart (cicero_scores) | P2 | M | CRT-84 | TODO |
| 4 | [CRT-86](CRT-86.md) | Stats chart redesign — game log + season averages | P2 | M | CRT-84 (after 85) | TODO |
| 5 | [CRT-87](CRT-87.md) | Real news feed + per-section error isolation | P2 | M | CRT-84 (after 85, 86) | TODO |
| 6 | [CRT-88](CRT-88.md) | Player page investment-lens review | P2 | S | CRT-85, 86, 87 | TODO |
| 7 | [CRT-89](CRT-89.md) | Watchlist wiring + invest handoff stub | P2 | M | CRT-84 (88 sign-off recommended) | TODO |
| 8 | [CRT-78](CRT-78.md) | Settings dialog — theme + account | P3 | S-M | none (anytime) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **CRT-84 first, always.** At least 10 real players' pages currently render "Player not found" because hand-written Zod schemas reject NULLs the DB allows. Every other player-page ticket would be verified against broken pages without it.
- **CRT-85 → 86 → 87 in sequence**, not because of logic coupling but because all three edit `app/(main)/players/[id]/page.tsx`; serializing avoids merge churn. Each plan's drift check covers this.
- **CRT-88 is a review of the finished page** — running it before 85/86/87 wastes the ticket.
- **CRT-89 needs operator action mid-plan**: applying the `watchlist` table migration to the production DB. Code steps can proceed while waiting.
- **CRT-78 is fully independent** — good filler while blocked on anything above.
- **External blockers to chase with Bryan (Combine)**: `cicero_scores` is empty (blocks CRT-85's populated-path verification; CRT-83 AC partially blocked) and has no game/date column besides `calculated_at`; `player_stats.is_mock` has zero `true` rows. Details in `plans/CRT-84-report.md` after CRT-84 runs.

## Findings considered and rejected / deferred (so nobody re-audits them)

- **News "Load more" pagination (CRT-87 AC)**: deferred — the news API schema has no cursor/date field; doing it right means a TanStack Query rework of the section. Flagged for a follow-up issue in the CRT-87 plan.
- **"Compare against league average" (CRT-86 AC)**: skipped — no league-average data exists anywhere in the DB or pipeline.
- **Literal tabbed layout (CRT-78)**: replaced with two titled sections — `@radix-ui/react-tabs` is not installed and new deps need approval; all ACs still met.
- **"Invested state" on the invest button (CRT-89 AC)**: deferred to CRT-90 — `transactions` has 0 rows and no buy flow exists to create positions.
- **Joining `player_stats.prScore` as a stand-in for `cicero_scores` history (CRT-85)**: rejected — different metric lineage; explicitly a STOP condition in that plan.
- **Out-of-scope observations** (candidates for Linear issues, not fixed by any plan): `updateUserProfile` server action throws instead of returning `{ data, error }` (violates CLAUDE.md error convention); `fetchPlayerData` caches only 10 active players for 24h (`lib/data/players.ts:23-54`) — explore page shows a hardcoded subset; `vitest` sits in `dependencies` instead of `devDependencies`; the player route is `[id]` while tickets say `[slug]`.
